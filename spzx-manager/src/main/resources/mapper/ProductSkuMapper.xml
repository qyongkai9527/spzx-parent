<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.joker.spzx.manager.mapper.ProductSkuMapper">

    <select id="selectByProductId" resultType="com.joker.spzx.model.entity.product.ProductSku">
        select  id,
                sku_code,
                sku_name,
                product_id,
                thumb_img,
                sale_price,
                market_price,
                cost_price,
                stock_num,sku_spec,
                weight,
                volume,
                status,create_time,
                update_time,is_deleted
        from product_sku
        where product_id = #{productId}
        and is_deleted = 0
        order by id desc
    </select>
</mapper>
